# TESTING.md

リリース前と、大きな変更を加えたあとの動作確認手順です。毎回このとおりに実行します。

対象バージョン: 1.1.8 / 出力先: `wp-content/uploads/distan/dist/`

---

## 0. 事前準備

- [ ] 旧バージョン・旧名称のプラグインを**停止ではなく「削除」**する
      （停止だけではオプションが DB に残り、切り分けが濁ります）
- [ ] `wp-config.php` にデバッグ設定を入れる

```php
define( 'WP_DEBUG', true );
define( 'WP_DEBUG_LOG', true );
define( 'WP_DEBUG_DISPLAY', false );
```

- [ ] キャッシュ系プラグインが**無効**であることを確認する
      （有効だと古いキャッシュ HTML を取得し、編集内容が反映されない無言の失敗が起きます）
- [ ] パーマリンクが「基本」以外であることを確認する

---

## 1. 有効化

- [ ] ZIP をアップロードして有効化。致命的エラーが出ない
- [ ] `wp-content/uploads/distan/dist/` と `.../work/` が作成されている
      （アクティベーションフックで作られます）

---

## 2. 管理画面

- [ ] 左メニューに **Distan** が表示される
- [ ] 管理画面を開き、カードが 3 つ（環境 / 生成 / 設定）描画される
- [ ] H1 直下に追従ナビ（環境 / 生成 / 設定）が表示され、スクロールしても管理バー直下に貼り付く
- [ ] リンクを押すと該当セクションへジャンプし、見出しが管理バー・追従ナビに隠れない（`scroll-margin`）
- [ ] スクロールに応じて現在地リンクがハイライトされる（Alpine スクロールスパイ）

> カードが出ない場合は Alpine.js が動いていません。ブラウザのコンソールを確認してください。
> アセットのファイル名を変更した直後は、`enqueue` のパスが追随しているかを疑います。

---

## 3. 設定

- [ ] 公開 URL を入力して保存 → リロードして値が残る
- [ ] ファイル名の形式・内部リンクの書き方・検索エンジンの扱いが保存される

---

## 4. 環境チェック

- [ ] 「確認する」で 5 項目が表示される
- [ ] **ループバック通信が OK**（これが NG なら以降は進めません）
- [ ] 出力先ディレクトリ・画像ライブラリ・実行時間が想定どおり

---

## 5. 生成

- [ ] 「静的 HTML を書き出す」でプログレスバーが最後まで進む
- [ ] 「書き出せなかったページ」が 0 件
- [ ] 統計タイル（出力ファイル / 追加 / 出力先から削除 / リンク切れ）が表示される

---

## 6. 出力検証

```bash
cd wp-content/uploads/distan/dist
```

### 6-1. クリーナーが作動したか（最重要）

```bash
grep -rl 'name="generator"' . ; grep -ril noindex .
```

- [ ] **両方とも出力ゼロ**

> ここが最も重要です。クリーナーの判定（`X-Distan-Render` ヘッダと共有シークレット）が壊れていると、
> **生成は成功したように見えるのに出力だけが汚れます。** エラーが出ないため、この確認でしか気づけません。

### 6-2. パス平坦化

```bash
ls
find media -type f | head
```

- [ ] `wp-content/` が**存在しない**
- [ ] `assets/`（テーマ）と `media/`（アップロード）がある
- [ ] `media/` にファイルがコピーされている

### 6-3. 404

```bash
grep -o 'href="[^"]*"' 404.html | head
```

- [ ] `404.html` が生成されている
- [ ] リンクが**絶対 URL**（公開 URL）になっている
      （404 は任意の階層で表示されるため、相対では壊れます）

### 6-4. キャッシュバスティング

```bash
grep -rho 'href="[^"]*\.css[^"]*"' . | sort -u
```

- [ ] テンプレートがクエリを付けている場合、`?v=` 等が**残っている**
- [ ] `dist/` 内の実ファイル名にはクエリが付いていない（上書きできること）

### 6-5. canonical / OGP

```bash
grep -o '<link[^>]*canonical[^>]*>' about/index.html
```

- [ ] 公開 URL を指している（ローカルのホスト名が残っていない）

```bash
grep -rl 'localhost\|\.local/' . | head
```

- [ ] 開発環境のホスト名が残っていない

---

## 7. ダウンロード

- [ ] 「ZIP をダウンロード」で ZIP が落ちる
- [ ] ZIP のルートに `distan-report.md` があり、**`dist/` 本体には入っていない**
- [ ] 「差分レポート（.md）」で Markdown が落ちる
- [ ] レポートに「本番から削除が必要なファイル」「リンク切れ」が正しく載っている

### 7-1. テンプレート書き出し ★1.5.0

生成画面の「テンプレート書き出し」（設定でオン・既定オン）でページを 1 枚選んで書き出す。

- [ ] 選んだページ 1 枚と、そのページが参照するアセット（CSS・JS・フォント・画像）だけが ZIP に入っている
- [ ] **選んだページが参照しないアセット（他ページ専用の画像など）は入っていない**（全アセット丸ごと同梱になっていないこと）
- [ ] スタイルシートの `url()` / `@import` が指すフォント・画像も含まれている（CSS 追従の確認）
- [ ] ナビ等リンク先の他ページは含まれない（1 枚納品では切れて当然）
- [ ] ZIP のルートに制作者向け `README.md` があり、`生成日時` と `書き出し日時` の両方が載っている
- [ ] 解凍してブラウザで開き、ヘッダー・フッターの見た目が本番と揃う（参照アセットが解決している）
- [ ] **設定で本番 URL やリンク書式を変えてから書き出しても、生成時の値で解決される** — 絶対 URL 指定モードで生成 → 設定の本番 URL を別の値に変更 → テンプレート書き出し、でアセットが解決すること（consumer が live 設定ではなく manifest 由来で成果物を解釈している確認。設計上の前提 5）

### 7-1b. テンプレート書き出し — マーカーによる除外 ★1.6.0

テンプレートページの HTML にマーカーを書いた状態で「このページで書き出す」。

- [ ] `<!-- distan:no-block-styles -->` を書くと、雛形から `wp-block-library` / `wp-block-library-theme` の `<link>` と `global-styles` のインライン `<style>` が消える
- [ ] あわせて `wp-block-library-inline-css` や `wp-block-*-inline-css`、`*-placeholder-inline-css`、`wp-img-auto-sizes-*` などの**ブロック系インライン `<style>` も全て消える**（id が `wp-block-` / `wp-img-auto-sizes` 始まり、または `global-styles` 含み）
- [ ] テーマ本体のインライン `<style>`（id が上記に該当しないもの）は**残る**
- [ ] `<!-- distan:drop-assets wp-includes/ -->` を書くと、`wp-includes/` 配下を指す `<script>` / `<link>` が雛形から消える（ファイルも同梱されない）
- [ ] `wp-content/plugins/プラグイン名/` を指定すると、そのプラグインのスクリプト/スタイルだけが消える
- [ ] 外した参照はタグごと消えている（雛形を開いても 404 参照が残らない）
- [ ] マーカーのコメント（`<!-- distan:... -->`）は、渡す雛形 HTML から除去されている
- [ ] 残したスタイルシートが内部で読む url() のフォント・画像は、除外指定に一致しても消えない（残した CSS が壊れない）
- [ ] 同梱 README.md に、除外した内容（no-block-styles / drop-assets の指定）が記録されている
- [ ] マーカーが無いページは、従来どおり丸ごと（ブロックCSS含む）書き出される

---

### 7-2. 取りこぼしの取り込み ★1.6.0

コア sitemap にあって未生成の URL が出る状態を作る（例: 一覧に出ないカスタム投稿タイプや、プラグインが登録するページ）。生成後、「取りこぼしの取り込み」パネルが出る。

- [ ] コア sitemap にあって今回未生成の URL が候補として並ぶ
- [ ] 既定ではどれも「未決」で、**何も選ばずに生成しても、候補 URL は生成物に含まれない**（オプトインの確認）
- [ ] 「含める」にして保存 → 次に生成すると、その URL が生成され、差分にも出る
- [ ] 生成された取り込み URL は、次回の候補一覧から消える（`sitemap_missing` から外れる）が、自由入力欄には残っている
- [ ] 「今後表示しない」にして保存 → その URL が候補一覧から消え、折りたたみの「今後表示しないにした URL」に入る
- [ ] 折りたたみで「候補に戻す」→ 保存 → 再び候補（未決）に戻る
- [ ] 自由入力欄に **このサイト外の URL** を書いて保存 → 取り込まれない（同一オリジンのみ許可の確認）
- [ ] 自由入力欄にサイト内の URL を書いて保存 → 次の生成に含まれる
- [ ] 保存した判断（含める／今後表示しない）は、別の生成をまたいでも保持される

---

## 8. 表示確認

```bash
cd wp-content/uploads/distan/dist
python3 -m http.server 8000
```

- [ ] `http://localhost:8000/` でトップが表示される
- [ ] CSS が効いている
- [ ] 画像が表示される
- [ ] 下層ページへのリンクが機能する
- [ ] 下層ページからトップへ戻れる（相対パスの深さが正しい）

> `file://`（ダブルクリック）でも HTML・CSS・画像・リンクは動きますが、
> ブロックテーマの ES modules はブラウザの CORS 仕様により読み込めません。
> 完全な確認にはローカルサーバーを使ってください。

---

## 9. 再生成と差分

- [ ] 投稿を 1 件追加して再生成 → 「追加」に反映される
- [ ] 投稿を 1 件削除して再生成 → 「本番から削除が必要なファイル」に出る
- [ ] 出力ディレクトリから当該ファイルが**自動で消えている**

### 9-1. 由来（provenance）ラベル ★1.1.7

差分レポートの追加/削除行が、ファイルパスだけでなく**ラベル＋由来タグ**で出ることを確認します。

- [ ] 生成 → 「差分レポート（.md）」の追加行が `ラベル [種別 #id]` 形式
      （例: `フロントページ [フロントページ]` / タイトル `[投稿 #123]` / `News [タクソノミー #7]`）
- [ ] 投稿を 1 件**タイトル変更**して再生成 → 差分にそのページがラベル付きで出る
- [ ] 投稿を 1 件削除して再生成 → 「削除が必要」欄に**ラベル付き**で出る
      （削除ページのラベルは前回マニフェストの `entries` から繰り越されます。壊れるとパスだけになります）

```bash
cd wp-content/uploads/distan/dist
grep -nE '\[(投稿|タクソノミー|フロントページ|投稿アーカイブ|外部ソース)' distan-report.md | head
```

---

## 10. 拡張ポイント（`distan_sources` / `distan_collect` / コアsitemap）★1.1.7

フィルタを一時的に足して確認します。`wp-content/mu-plugins/distan-test.php` を作成:

```php
<?php
// A) カスタム URL ソース（列挙が発見できない URL を第一級で追加）
add_filter( 'distan_sources', function ( $providers ) {
	$providers[] = function () {
		return array(
			Distan_Collector::make_item(
				home_url( '/__test-virtual/' ),
				'テスト仮想ページ',
				array( 'kind' => 'extra', 'origin' => 'test' )
			),
		);
	};
	return $providers;
} );

// B) distan_collect でトップと衝突する URL を差し込む（上書き防止の確認）
add_filter( 'distan_collect', function ( $queue ) {
	$queue[] = Distan_Collector::make_item( home_url( '/' ), '衝突テスト', array( 'kind' => 'extra' ) );
	return $queue;
} );
```

### 10-1. distan_sources（カスタム URL）

- [ ] 再生成 → 統計タイルの「出力ファイル」が 1 件増える
- [ ] `dist/__test-virtual/index.html` が存在する
- [ ] レポートに `[外部ソース]` で載る

### 10-2. distan_collect の衝突（上書き防止＝1.1.7 の修正）

- [ ] トップが「衝突テスト」で**上書きされていない**（built-in のフロントページが勝つ）
- [ ] `index.html` が二重生成されていない

```bash
cd wp-content/uploads/distan/dist
find . -name index.html | grep -c '^\./index.html$'   # 1 であること
```

### 10-3. コアsitemap 突き合わせ（診断・既定オン）

- [ ] `/wp-sitemap.xml` がローカルで開ける（コアsitemap が有効）
- [ ] 生成 → レポートに `## コア sitemap にあって未生成のURL` 節。既定除外の著者/日付や、プラグイン登録 URL があれば列挙される
      （ギャップが無ければ節は出ない。それも正常）

### 10-4. コアsitemap シード（選択制・既定オフ）

```php
add_filter( 'distan_use_core_sitemap', '__return_true' );
```

- [ ] 再生成 → コアsitemap の URL が列挙に合流して生成される／10-3 の「未生成」節が縮む・消える／重複生成が増えない
- [ ] 確認後、この filter を外す

### 10-5. 後片付け

- [ ] `wp-content/mu-plugins/distan-test.php` を削除 → 最終クリーン生成
- [ ] `wp-content/debug.log` に新規のワーニング/ノーティスが無い
      （`make_item` の source、`provided_items`、`write_manifest` の `entries`、sitemap 監査は毎回走るため、ここは必ず確認）

---

## 11. アンインストール

- [ ] プラグインを**削除**する
- [ ] `distan_settings` などのオプションが DB から消えている
- [ ] `dist/` の生成物は**残っている**（意図した挙動）

---

## 症状別の切り分け

| 症状 | 考えられる原因 | 確認するところ |
| --- | --- | --- |
| 管理画面のカードが描画されない | Alpine.js が読み込まれていない | ブラウザのコンソール、`enqueue` のファイル名と依存関係 |
| `SyntaxError: Unexpected token '<'` | PHP の致命的エラー（HTML が JSON の代わりに返っている） | `wp-content/debug.log` |
| 生成は成功するが `generator` タグが残る | クリーナーの判定が壊れている | `HEADER_NAME` と `REQUEST_HEADER` のペア、`distan_render_secret` |
| ループバックが 401 / 403 | Basic 認証がかかっている | 生成中は認証を外す |
| 画像が表示されない | 平坦化と逆引きの不整合 | `media/` の実ファイルと HTML の参照を突き合わせる |
| `wp-content/` が残る | 平坦化が効いていない | `distan_uploads_dir` / `distan_flatten_theme` |
| ZIP ボタンが出ない | `ZipArchive` が無効 | 環境チェックの表示 |
| リンク切れが大量に出る | 対象外のアーカイブへテーマがリンクしている | 仕様。テーマ側で出さないか、`distan_taxonomies` 等で対象に加える |
| 生成物が古い | キャッシュ系プラグインが有効 | 無効化して再生成 |

---

## 大規模な変更のあとに追加で確認すること

名称変更・プレフィックス変更・ファイル名変更を行った場合、**対になっている識別子がずれていないか**を照合します。片方だけ変わっても構文エラーにならないため、動作で初めて気づくことになります。

```bash
# 1. レンダー判定ヘッダ（送信側と受信側）
grep -rn "HEADER_NAME\|REQUEST_HEADER" includes/*.php

# 2. AJAX アクション（PHP 側と JS 側）
grep -rho "wp_ajax_distan_[a-z_]*" includes/*.php
grep -o "'distan_[a-z_]*'" assets/js/distan-admin.js

# 3. nonce 名（生成側と検証側）
grep -rho "wp_create_nonce( '[a-z_]*' )\|check_admin_referer( '[a-z_]*' )" includes/*.php

# 4. admin-post アクション（登録側と URL 側）
grep -rho "admin_post_distan_[a-z_]*\|action=distan_[a-z_]*" includes/*.php

# 5. 旧名称の残存
grep -rin "旧名称" .
```

さらに、**クラスをまたぐ静的呼び出し**に注意します。`normalize()` や `is_contained()` は `Distan_Paths` にあり、他クラスから `self::` では呼べません。この種のミスは構文チェックを通過し、実行時に初めて致命的エラーになります。
